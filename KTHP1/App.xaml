<Application x:Class="KTHP1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KTHP1"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="tbStyle" TargetType="TextBox">
            <Setter Property="Width" Value="250"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <ControlTemplate x:Key="btnTem" TargetType="Button">
            <Grid>
                <Border Width="100" Height="30"
                        CornerRadius="15" Margin="5"
                        BorderThickness="2" BorderBrush="Green" Background="Cyan"
                        HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </ControlTemplate>

        <Style x:Key="labelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Time New Roman"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Application.Resources>
</Application>
